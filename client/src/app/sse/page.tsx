import React from 'react';
import EventStream from "@/components/event-stream";

const Page = () => {
    return (
        <div>
            <EventStream/>
        </div>
    );
};

export default Page;